import React from 'react'
import ResourceForm from '../Forms/ResourceForm';

const UserSettings = () => ({
  render() {
    return (
      <h1>User Settings</h1>
    )
  }
})

export default UserSettings;